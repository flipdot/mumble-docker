# Murmur configuration file.
# See https://wiki.mumble.info/wiki/Murmur.ini for more options

# Path to database. If blank, will search for
# murmur.sqlite in default locations or create it if not found.
database=/etc/murmur/murmur.sqlite

# If Murmur is started as root, which user should it switch to?
# This option is ignored if Murmur isn't started with root privileges.
uname=murmur

sslCert=/etc/murmur/fullchain.pem
sslKey=/etc/murmur/privkey.pem

users=200

bandwidth=130000
suggestPushToTalk=true

registerName=flipdot
welcometext="Willkommen im virtuellen flipdot!"
defaultchannel=8
rememberchannel=false
